#include "main.h"

/**
 * add - function add two integers and return the result
 * @x: is an integer
 * @y: is an integer
 *
 * Return: the sum of two integers.
 */
int add(int x, int y)
{
	return (x + y);
}
