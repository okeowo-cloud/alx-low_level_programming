#include "main.h"

/**
 * mul - function multiplies two integers.
 * @a: is an integer
 * @b: is an integer
 *
 * Return: the product of two numbers
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
